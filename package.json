{
  "name": "yume-spec",
  "version": "1.2.0",
  "description": "Embed spec-why (@why) in code; auto-injects the convention into pi each turn; yspec/yhist/yui overview tools + spec collision checker + Web UI health inspector (yume-min/ai-desk family).",
  "type": "module",
  "exports": {
    ".": "./extensions/yume-spec.ts",
    "./ui": "./ui/index.js",
    "./collision": "./collision.js"
  },
  "scripts": {
    "test": "node test.mjs"
  },
  "keywords": [
    "pi-package",
    "yume",
    "yume-min",
    "ai-first",
    "spec",
    "docs-as-code",
    "ui-graph",
    "ui-health"
  ],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "dependencies": {
    "playwright-core": "^1.55.0"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "license": "MIT"
}
